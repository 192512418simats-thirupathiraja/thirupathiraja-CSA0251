#include <stdio.h>
int pal(int n,int rev){
    if(n==0) return rev;
    return pal(n/10,rev*10+n%10);
}
int main(){
    int n;
    scanf("%d",&n);
    if(n==pal(n,0)) printf("Palindrome");
    else printf("Not");
    return 0;
}
