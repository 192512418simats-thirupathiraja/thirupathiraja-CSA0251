#include <stdio.h>
int main(){
    int n,sum=0,prod=1,rem;
    scanf("%d",&n);
    while(n){
        rem=n%10;
        sum+=rem;
        prod*=rem;
        n/=10;
    }
    if(sum==prod) printf("Spy");
    else printf("Not Spy");
    return 0;
}
