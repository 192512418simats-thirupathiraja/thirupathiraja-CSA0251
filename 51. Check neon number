#include <stdio.h>
int main(){
    int n,sum=0,rem,sq;
    scanf("%d",&n);
    sq=n*n;
    while(sq){
        rem=sq%10;
        sum+=rem;
        sq/=10;
    }
    if(sum==n) printf("Neon");
    else printf("Not Neon");
    return 0;
}
